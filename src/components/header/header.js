import React from 'react';
import NavBar from '../navBar/navBar.js';

function Header() {
    return (
        <div className="header">
            <NavBar/>
        </div>
    );
}

export default Header;